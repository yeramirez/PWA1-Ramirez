/*
	* Mid Terms for PWA-1
*/

//self executing function that loads first student
    //array variable defined
    //array variable contains the object list of student information

    //starting at array index 0, display student[0]'s object information
    //pull that students information and display
    //using document.write I display students object attributes

    //define button loop. var button = myarray.length
    //when button is pressed loop to next object in array. IE next student
    //console log and document write students[i]'s information
    //create if statement to reloop array through student log.
        //student[finalstudent] becomes student[0].